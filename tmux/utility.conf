# Display lazygit
bind -r g display-popup -d '#{pane_current_path}' -w80% -h80% -E lazygit

# bind -r a display-popup -d '#{pane_current_path}' -w80% -h80% -E "tmux attach -t claudecode || tmux new -s opencode opencode --continue"

# Run claudecode in a separate session
# Replaced by tmux-claude-session-manager (loaded from tmux.conf), which binds
# prefix+y (launch) and prefix+u (agent picker).
# bind -r y run-shell '\
#   SESSION="claudecode-$(echo #{pane_current_path} | md5sum | cut -c1-8)"; \
#   tmux has-session -t "$SESSION" 2>/dev/null || \
#   tmux new-session -d -s "$SESSION" -c "#{pane_current_path}" "claudecode"; \
#   tmux display-popup -w80% -h80% -E "tmux attach-session -t $SESSION"'
